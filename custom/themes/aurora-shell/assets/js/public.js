// Aurora shell public JS (intentionally lightweight for v1).
(() => {
    if (document.documentElement) {
        document.documentElement.setAttribute('data-theme-shell', 'aurora-public');
    }
})();
